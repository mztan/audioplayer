<Page
    x:Class="AudioPlayerSample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AudioPlayerSample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="30" Text="Audio Player Sample" HorizontalAlignment="Center" Margin="40"/>

        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Grid.Row="1">
            <AppBarButton Label="alarm" Icon="Play" Click="Alarm_Click"/>
            <AppBarButton Label="notify" Icon="Play" Click="Notify_Click"/>
            <AppBarButton Label="ring" Icon="Play" Click="Ring_Click"/>
        </StackPanel>

    </Grid>
</Page>
